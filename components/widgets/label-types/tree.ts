export type PhiTreeWidgetLabels = {
  search: string;
  reset: string;
  reload: string;
  empty: string;
};

export const PHI_TREE_WIDGET_DEFAULT_LABELS: PhiTreeWidgetLabels = {
  search: "Search",
  reset: "Reset",
  reload: "Reload",
  empty: "No nodes found.",
};
