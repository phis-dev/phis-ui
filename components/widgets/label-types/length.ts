export type PhiLengthWidgetLabels = {
  title: string;
  description: string;
  placeholder: string;
};

export const PHI_LENGTH_WIDGET_DEFAULT_LABELS: PhiLengthWidgetLabels = {
  title: "Length",
  description: "Numeric CSS length control with an explicit unit.",
  placeholder: "Length",
};
