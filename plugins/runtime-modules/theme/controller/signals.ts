export const PHI_THEME_SIGNAL_CHANNELS = {
  brandTheme: "style",
  draftStatus: "draftStatus",
  previewThemeMode: "brandPreviewThemeMode",
  command: "command",
  presetSelect: "select",
} as const;
