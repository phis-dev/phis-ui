export type PhiImageWidgetLabels = {
  emptyTitle: string;
  emptyDescription: string;
};

export const PHI_IMAGE_WIDGET_DEFAULT_LABELS: PhiImageWidgetLabels = {
  emptyTitle: "Image",
  emptyDescription: "No image source configured.",
};
